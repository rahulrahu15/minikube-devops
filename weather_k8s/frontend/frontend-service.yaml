apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: weather-frontend
    version: green # change this between blue/green to switch traffic
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort
